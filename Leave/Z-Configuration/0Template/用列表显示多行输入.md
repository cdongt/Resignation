---
Tags:
<%*
let multiLineInput = await tp.system.prompt("Please input the tags", "", true, true);
let lines = multiLineInput.split("\n"); // Split multi-line text into an array.
lines.forEach(line => {
    if (line.trim() !== "") {
        tR += `- ${line}\n`; // Each line is preceded by a list symbol.
    }
});
%>
---